apiVersion: apps/v1
kind: Deployment
metadata:
  name: cp-demo-1778503916
  labels:
    app: cp-demo-1778503916
    market: usa
    managed-by: backstage-scaffolder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cp-demo-1778503916
  template:
    metadata:
      labels:
        app: cp-demo-1778503916
        market: usa
    spec:
      containers:
        - name: app
          # Placeholder until first CI build pushes a real image.
          # `nginx-unprivileged` listens on 8080 (matches Java/Python/Node runtimes).
          # CD pipeline patches `image:` to ghcr.io/priyankaajbhagatt-collab/cp-demo-1778503916:<sha>.
          image: nginxinc/nginx-unprivileged:1.27-alpine
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
